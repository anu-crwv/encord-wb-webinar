# secrets — never commit
CWKubeconfig_*
*.kubeconfig
.env
.env.*
*.pem
*.key
# python
__pycache__/
*.pyc
*.egg-info/
.venv/
venv/
# large local artifacts (live on cluster PVC / W&B). Anchored to the repo root with a leading slash
# so they do NOT match source dirs like groot/vla/data/ (the dataset loader package must be committed).
/checkpoints/
/data/
/exports/
wandb/
*.mp4
# os/editor
.DS_Store
.idea/
.vscode/
*.swp
exports/
/AGENTS.md

# local Encord webinar administration and generated migration state
/ENCORD_WEBINAR_REPORT.md
/scripts/encord/data-registration/apply_*_workspace_cleanup.cjs
/scripts/encord/data-registration/encord_*_report.json
/scripts/encord/data-registration/encord_*_state.json
