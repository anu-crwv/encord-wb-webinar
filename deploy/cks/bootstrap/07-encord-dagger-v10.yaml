# Assemble v10 = real(v8) + DAgger scripted-expert demos (x WAM_DEMO_DUP), recompute stats +
# step_filter, log demos as a W&B artifact. CPU node. Prereq: demo shards under
# /data/wam/datasets/dagger_demos/{batt,cyl}/shard-*.
apiVersion: batch/v1
kind: Job
metadata:
  name: wam-encord-dagger-v10
  namespace: dreamzero
  labels: {app.kubernetes.io/part-of: dreamzero, dreamzero/gate: "wam-data"}
spec:
  backoffLimit: 0
  ttlSecondsAfterFinished: 172800
  template:
    metadata:
      labels: {app.kubernetes.io/part-of: dreamzero, dreamzero/gate: "wam-data"}
    spec:
      restartPolicy: Never
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
              - matchExpressions:
                  - {key: nvidia.com/gpu.product, operator: DoesNotExist}
                  - {key: kubernetes.io/arch, operator: In, values: [amd64]}
      securityContext: {runAsNonRoot: true, runAsUser: 1000, runAsGroup: 1000, fsGroup: 1000, seccompProfile: {type: RuntimeDefault}}
      containers:
        - name: assemble
          image: python:3.12-slim
          command: ["/bin/bash", "-exo", "pipefail", "-c"]
          args:
            - |
              export HOME=/data/.home-amd64 PIP_CACHE_DIR=/data/.home-amd64/.pip TMPDIR=/data/.home-amd64/.tmp
              export WANDB_DIR=/data/.home-amd64/.wandb WAM_REPO_ROOT=/data/src/dreamzero-wam
              mkdir -p $HOME $PIP_CACHE_DIR $TMPDIR $WANDB_DIR
              python -m pip install --no-cache-dir --quiet "wandb>=0.18.0" pandas pyarrow numpy tqdm
              cd $WAM_REPO_ROOT
              python scripts/data/assemble_dagger_v10.py
              python - <<'PY'
              import json
              d=json.load(open("/data/wam/datasets/encord_trossen_v10/meta/info.json"))
              print("=== v10:", d["total_episodes"],"eps", d["total_frames"],"frames", "tasks",d["total_tasks"])
              assert d["total_episodes"]>1894 and d["fps"], d
              PY
              echo "=== v10 assemble done ==="
          envFrom: [{secretRef: {name: wandb-api-key}}]
          env:
            - {name: WANDB_ENTITY,  value: encord-wb-physical-ai}
            - {name: WANDB_PROJECT, value: wam-finetune-webinar}
            - {name: WAM_DEMO_DUP,   value: "8"}
            - {name: WAM_DEMO_GROUPS, value: "batt,cyl"}
          securityContext: {allowPrivilegeEscalation: false, readOnlyRootFilesystem: false, capabilities: {drop: ["ALL"]}}
          resources:
            requests: {cpu: "8", memory: 32Gi}
            limits:   {cpu: "16", memory: 96Gi}
          volumeMounts:
            - {name: data, mountPath: /data}
      volumes:
        - {name: data, persistentVolumeClaim: {claimName: dreamzero-data}}
