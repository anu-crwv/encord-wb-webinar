# Encord/Trossen LoRA fine-tune on the v6 dataset — SINGLE GH200 (arm64), DeepSpeed ZeRO-3 + CPU offload.
# The proven reliable single-node path, retargeted at the LARGE v6 dataset (~1,364 eps / ~2.5x v4's 531)
# intended to fix the data-starved action head. Steps scaled to the bigger data: 8000 (vs 3000 for v4).
# ~8000 steps x ~9.2s ≈ 20h single-GH200. (2-node data-parallel would ~halve wall-time; single-node
# chosen for reliability.)
#
# Naming: OUTPUT_DIR + the encord_trossen.sh entrypoint keep runs/artifacts from reading "droid".
# Prereq: /data/wam/datasets/encord_trossen_v6 built; repo staged at /data/src/dreamzero-wam; wandb secret.
# Inject a unique WANDB_RUN_ID at apply (sed WAMRUNIDPLACEHOLDER).
apiVersion: batch/v1
kind: Job
metadata:
  name: wam-encord-trossen-lora-v6aug
  namespace: dreamzero
  labels: {app.kubernetes.io/part-of: dreamzero, dreamzero/gate: "wam-train"}
spec:
  backoffLimit: 0
  ttlSecondsAfterFinished: 259200
  template:
    metadata:
      labels: {app: wam-train, app.kubernetes.io/part-of: dreamzero, dreamzero/gate: "wam-train"}
    spec:
      restartPolicy: Never
      nodeSelector:
        kubernetes.io/arch: arm64
        nvidia.com/gpu.product: NVIDIA-GH200-480GB
      tolerations:
        - {key: is_gpu, value: "true", operator: Equal, effect: PreferNoSchedule}
      securityContext: {runAsNonRoot: true, runAsUser: 1000, runAsGroup: 1000, fsGroup: 1000, seccompProfile: {type: RuntimeDefault}}
      containers:
        - name: train
          image: nvcr.io/nvidia/pytorch:25.04-py3
          imagePullPolicy: IfNotPresent
          command: ["/bin/bash", "-exo", "pipefail", "-c"]
          args:
            - |
              export HOME=/data/.home-gh200-0 PIP_CACHE_DIR=/data/.home-gh200-0/.pip
              export TMPDIR=/data/.home-gh200-0/.tmp HF_HOME=/data/.hf
              mkdir -p $HOME $PIP_CACHE_DIR $TMPDIR $HF_HOME
              export WAM_REPO_ROOT=/data/src/dreamzero-wam
              export PYTHONPATH=$WAM_REPO_ROOT:${PYTHONPATH:-}
              export PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True
              export NNODES=1 NUM_GPUS=1
              export TRITON_CACHE_DIR=$TMPDIR/triton; mkdir -p $TRITON_CACHE_DIR
              cd $WAM_REPO_ROOT
              python -m pip install --no-cache-dir --quiet --user -r requirements-train.txt
              DS_NVTX=$(find $HOME/.local/lib -path '*/deepspeed/utils/nvtx.py' 2>/dev/null | head -1)
              if [ -n "$DS_NVTX" ]; then sed -i 's/^enable_nvtx = True/enable_nvtx = False/' "$DS_NVTX"; echo "patched $DS_NVTX"; fi
              echo "trossen LoRA run_id=$WANDB_RUN_ID data=$DATA_CONFIG local=$WAM_DATASET_LOCAL_DIR out=$OUTPUT_DIR steps=$MAX_STEPS"
              bash scripts/train/encord_trossen.sh
          envFrom: [{secretRef: {name: wandb-api-key}}]
          env:
            - {name: WANDB_ENTITY,   value: encord-wb-physical-ai}
            - {name: WANDB_PROJECT,  value: wam-finetune-webinar}
            - {name: WAM_REGISTRY_ORG, value: encord-wb-webinar}
            - {name: WANDB_RUN_ID,   value: "WAMRUNIDPLACEHOLDER"}
            - {name: OUTPUT_DIR,     value: "/checkpoints/wam/runs/encord_trossen_lora_WAMRUNIDPLACEHOLDER"}
            - {name: MAX_STEPS,      value: "8000"}
            - {name: SAVE_STEPS,     value: "1000"}
            - {name: TRAIN_ARCHITECTURE, value: "lora"}
            - {name: LEARNING_RATE,  value: "1e-4"}
            - {name: PER_DEVICE_BATCH_SIZE, value: "1"}
            - {name: DEEPSPEED,      value: "configs/deepspeed/zero3_offload.json"}
            - {name: USE_AGIBOT_INIT, value: "0"}
            - {name: WAM_TRAIN_TAGS, value: "encord,trossen,lora,v6-data,strong-aug,sim2real,8000-steps"}
            # --- trossen embodiment + local v6 (unregistered) dataset ---
            - {name: DATA_CONFIG,    value: "dreamzero/trossen_relative_strongaug"}
            - {name: DATA_ROOT_KEY,  value: "trossen_data_root"}
            - {name: WAM_DATASET_LOCAL_DIR, value: "/data/wam/datasets/encord_trossen_v6"}
            # Record the LATEST Encord export halves as INPUT lineage (train from merged local dir; don't upload it).
            - {name: WAM_DATASET_SOURCE_ARTIFACTS, value: "encord-wb-physical-ai/wam-finetune-webinar/encord-source-data:v5,encord-wb-physical-ai/wam-finetune-webinar/encord-captions:v6"}
            - {name: WAM_CKPT_ARTIFACT_NAME, value: "dreamzero-trossen-lora"}
            - {name: VIDEO_BACKEND,  value: ""}
          securityContext: {allowPrivilegeEscalation: false, readOnlyRootFilesystem: false, capabilities: {drop: ["ALL"]}}
          resources:
            requests: {cpu: "16", memory: 200Gi, nvidia.com/gpu: "1"}
            limits:   {cpu: "64", memory: 460Gi, nvidia.com/gpu: "1"}
          volumeMounts:
            - {name: data,        mountPath: /data}
            - {name: checkpoints, mountPath: /checkpoints}
            - {name: dshm,        mountPath: /dev/shm}
      volumes:
        - {name: data,        persistentVolumeClaim: {claimName: dreamzero-data}}
        - {name: checkpoints, persistentVolumeClaim: {claimName: dreamzero-checkpoints}}
        - {name: dshm,        emptyDir: {medium: Memory, sizeLimit: 64Gi}}
